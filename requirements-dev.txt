# Local development and test tooling. The ASGI server lives in requirements.txt
# because it is the deployed start command, not because anything imports it.
-r requirements.txt
pytest>=8.0.0
beautifulsoup4>=4.12.3   # utils/ scrapers, run offline; no endpoint imports them
